<UserControl x:Class="PhoneCamWithAndroidCam.Views.PipelineChooserView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodel="clr-namespace:PhoneCamWithAndroidCam.ViewModels"
        mc:Ignorable="d"
        Height="155"
        Width="300"
        d:DataContext="{d:DesignInstance viewmodel:PipelineChooserViewModel}">

    <Grid>
        <ListBox
            ItemsSource="{Binding PipelinesName}"
            SelectedItem="{Binding SelectedPipeline}">
        </ListBox>
    </Grid>
</UserControl>
